spring:
  datasource:
    url: jdbc:mysql://localhost:3306/employee_system
    username: root
    password: PASSWORD
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true